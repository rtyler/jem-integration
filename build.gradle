apply plugin: 'maven'
apply plugin: 'groovy'

if (System.env['Jenkins.Repository']) {
    apply from: 'gradle/integration.gradle'
}

repositories {
    jcenter()
}

dependencies {
    compile 'com.github.jruby-gradle:jem:[0.3.0,1.0)'
}
